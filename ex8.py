formatter = "{} {} {} {}"

print(formatter.format(1, 2, 3, 4))
print(formatter.format("one", "two", "three", "four"))
print(formatter.format(True, False, False, True))
print(formatter.format(formatter, formatter, formatter, formatter))
print(formatter.format(
    "Kao nesto",
    "Treba da",
    "Upisem ovde",
    "Mogu sta hocu, a to sto mi nije jasno formatter.format i ko koga tu formatizuje",
    "nikom nista"
))

### pomagaj Borkoli, nista jasno mi NIJE :D zasto formatter.format zbog cega, zbog zasto